UnoCSS installieren!

cd ordner
npm create vite@latest . -- --template react-ts
npm install -D unocss
code .
npm run dev


1. Hast du UnoCSS in vite.config.ts eingebunden?

// vite.config.ts
import UnoCSS from 'unocss/vite'
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [
    react(),
    UnoCSS(), // << MUSS da rein!
  ]
})


2. Fehlt der Import von index.css in main.tsx?
// src/main.tsx
import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.tsx'
import './index.css' // << wichtig!
import 'uno.css' // << das auch, wenn du kein CLI-Build nutzt

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)

3. Ist uno.config.ts korrekt konfiguriert?
// uno.config.ts
import { defineConfig } from 'unocss'
import { presetWind3 } from '@unocss/preset-wind3'

export default defineConfig({
  presets: [
    presetWind3(),
  ]
})

4. Index CSS 
/* src/index.css */
@import '@unocss/reset/tailwind.css';


5. Fehlt ein UnoCSS Build/Dev Step?
npm run dev

6. (Optional) Sicherstellen, dass Vite JSX nicht cached
rm -rf node_modules .vite dist
npm install
npm run dev

----------------

✅ App.tsx mit UnoCSS-Beispiel
// src/App.tsx

export default function App() {
  return (
    <div className="min-h-screen bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center text-white">
      <div className="bg-white/10 backdrop-blur-md p-8 rounded-2xl shadow-xl text-center">
        <h1 className="text-4xl font-bold mb-4">👋 Hallo UnoCSS!</h1>
        <p className="text-lg">Dieses Styling kommt komplett von UnoCSS.</p>
        <button className="mt-6 px-4 py-2 bg-white text-blue-600 font-semibold rounded hover:bg-blue-100 transition">
          Los geht's
        </button>
      </div>
    </div>
  );
}